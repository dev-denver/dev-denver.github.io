name: Setup project
description: Setup Node and install dependencies (requires actions/checkout to have already run)

runs:
  using: composite
  steps:
    - name: Setup Node
      uses: actions/setup-node@v5
      with:
        node-version: "22"
        cache: "npm"
    - name: Install dependencies
      run: npm ci
      shell: bash
